Python project usage notes:

- Some of the files (CSV, Json) have been deleted due to GitHub file size constraints,
just the final ones have been kept. To create the others, run the daily routine
- Daily routine can be triggered by executing main function inside "main.py"
- To manage paths, change information in "setup.ini"
- To manage api credentials, change information in "setup.ini"
